# zrok-auto-install-ubuntu
Script to install zrok docker version for self-hosting on Ubuntu machine

To install zrok-docker type this in your machine's terminal and proceed with steps:

`bash <(curl -fsSL https://raw.githubusercontent.com/kazekagyeee/zrok-auto-install-ubuntu/main/install.sh)`
